name: Otimização Quinzenal de Portfólio v9.0

on:
  schedule:
    # Executar a cada 15 dias às 22:00 horário de Brasília (01:00 UTC)
    # Dias 1 e 16 de cada mês
    - cron: '0 1 1,16 * *'
  workflow_dispatch: # Permite execução manual

jobs:
  otimizar-portfolio:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout do código
      uses: actions/checkout@v3
    
    - name: Configurar Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Instalar dependências
      run: |
        pip install yfinance python-dotenv requests numpy pandas scikit-learn joblib pytz
    
    - name: Criar arquivo .env com secrets do bot
      run: |
        cat > backend/quantum-trades-backend/.env << EOF
        TELEGRAM_BOT_TOKEN=${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}
        EOF
    
    - name: Executar otimização quinzenal v9
      run: |
        cd backend/quantum-trades-backend
        python3 otimizador_quinzenal_v9.py
    
    - name: Commit mudanças no portfolio
      if: success()
      run: |
        git config --global user.name 'Magnus Bot'
        git config --global user.email 'bot@magnuswealth.com'
        git add backend/quantum-trades-backend/portfolio_config.json
        git diff --staged --quiet || git commit -m "🔄 Atualização automática do portfólio - $(date +'%Y-%m-%d')"
        git push || echo "Nenhuma mudança para commitar"
    
    - name: Verificar resultado
      if: always()
      run: |
        echo "Otimização v9 concluída!"
        echo "Verifique o Telegram para o relatório."
        echo "Portfolio atualizado em portfolio_config.json"
